write github commit using these guideline

- DON'T include "generated with claude", treat it as a human-authored commit
- Carefully review and respect manual commit intentions
- Avoid overwriting or automatically modifying human-created commits
- Use conventional commit format: `type(scope): description`